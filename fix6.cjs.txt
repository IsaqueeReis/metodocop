const fs = require('fs');
let code = fs.readFileSync('App.tsx', 'utf-8');

const target = "bg-zinc-700 text-zinc-400')}`}>{idx === 0 ?";
const replacement = "bg-zinc-700 text-zinc-400'))}`}>{idx === 0 ?";

if(code.includes(target)) {
    code = code.replace(target, replacement);
    fs.writeFileSync('App.tsx', code);
    console.log('Fixed parenthesis!');
} else {
    console.log('Target not found for parenthesis!');
}
