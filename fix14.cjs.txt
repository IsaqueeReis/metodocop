const fs = require('fs');
let code = fs.readFileSync('App.tsx', 'utf-8');

const target1 = `                                                <span className="font-mono font-bold text-red-500 text-lg">{count}</span>
                                            </div>

                                        {sortedByGoals.filter(item => getFilteredCount`;
const rep1 = `                                                <span className="font-mono font-bold text-red-500 text-lg">{count}</span>
                                            </div>
                                            )
                                        })}
                                        {sortedByGoals.filter(item => getFilteredCount`;

const target2 = `onClick={() => setViewingStudentId(u.id>
                                        <div className="flex items-center gap-3">`;
const rep2 = `onClick={() => setViewingStudentId(u.id)}>
                                        <div className="flex items-center gap-3">`;

if (code.includes(target1)) code = code.replace(target1, rep1);
else console.log("target1 not found");

if (code.includes(target2)) code = code.replace(target2, rep2);
else console.log("target2 not found");

fs.writeFileSync('App.tsx', code);
console.log('Fixed block 5');
