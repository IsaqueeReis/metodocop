const fs = require('fs');
let code = fs.readFileSync('App.tsx', 'utf-8');

const target = "<option key={s.id} value={s.name}>{s.name}</option>)})}</select>";
const replacement = "<option key={s.id} value={s.name}>{s.name}</option>)}</select>";

if(code.includes(target)) {
    code = code.replace(target, replacement);
    fs.writeFileSync('App.tsx', code);
    console.log('Fixed subjects.map inside select!');
} else {
    console.log('Target not found!');
}
